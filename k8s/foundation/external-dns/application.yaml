apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  name: external-dns
  namespace: argocd
spec:
  destination:
    name: in-cluster
    namespace: external-dns
  ignoreDifferences:
    - jsonPointers:
        - /spec/volumeName
        - /spec/storageClassName
        # Longhorn stores the expanded capacity in bytes (e.g. 10737418240) rather
        # than the human-readable form (e.g. 10Gi), causing ArgoCD to see a diff
        # and attempt a patch that Kubernetes rejects as reducing the storage request.
        - /spec/resources/requests/storage
      kind: PersistentVolumeClaim
    - jsonPointers:
        - /data/ip
      kind: ConfigMap
      name: ddns-public-ip
    - group: apiextensions.k8s.io
      kind: CustomResourceDefinition
  project: default
  source:
    path: k8s/foundation/external-dns
    repoURL: https://git.msng.to/ops/k8s-homelab
    targetRevision: main
  syncPolicy:
    automated:
      prune: true
      # selfHeal: false
    retry:
      backoff:
        duration: 1m
        factor: 2
        maxDuration: 16m
      limit: 10
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
      - RespectIgnoreDifferences=true
