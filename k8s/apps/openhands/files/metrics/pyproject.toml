[project]
name = "openhands-metrics"
version = "0.1.0"
description = "Prometheus exporter for OpenHands agent session token and cost usage"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]

[tool.pytest.ini_options]
pythonpath = ["."]
