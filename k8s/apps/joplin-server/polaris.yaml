exemptions:
  # POSTGRES_PASSWORD/POSTGRES_USER are sourced from a CNPG-generated
  # secretKeyRef, not a literal value, but polaris flags the env var name
  # regardless of value source — same false positive forgejo exempts.
  - controllerNames:
      - joplin-server
    rules:
      - sensitiveContainerEnvVar
