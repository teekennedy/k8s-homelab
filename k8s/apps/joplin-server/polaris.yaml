exemptions:
  # POSTGRES_PASSWORD/POSTGRES_USER come from a secretKeyRef, not a literal
  # value, but polaris flags the env var name regardless of source.
  - controllerNames:
      - joplin-server
    rules:
      - sensitiveContainerEnvVar
