apiVersion: v1
kind: Service
metadata:
  name: {{ include "archon.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "archon.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "archon.selectorLabels" . | nindent 4 }}
