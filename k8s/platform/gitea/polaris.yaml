exemptions:
  # configure-gitea init container sets GITEA_ADMIN_PASSWORD from a secretKeyRef, but polaris
  # flags the env var name regardless of value source. Cannot fix without patching upstream chart.
  - controllerNames:
      - gitea
    rules:
      - sensitiveContainerEnvVar
  # Helm test pod uses busybox without a pinned tag and runs as root.
  - controllerNames:
      - gitea-test-connection
    rules:
      - runAsRootAllowed
      - privilegeEscalationAllowed
      - tagNotSpecified
