apiVersion: v1
kind: Service
metadata:
  name: valkey
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: valkey
    app.kubernetes.io/component: valkey
spec:
  type: ClusterIP
  ports:
  - port: 6379
    targetPort: valkey
    protocol: TCP
    name: valkey
  selector:
    app.kubernetes.io/name: valkey
    app.kubernetes.io/component: valkey
